/// Breve nota do que mudou na versão atual — atualizada manualmente a cada
/// build enviada pra teste/produção, junto com o "version:" do
/// pubspec.yaml (esse sim é o número/build oficial, lido em runtime via
/// package_info_plus na tela "Atualizações").
class VersaoApp {
  static const String descricao =
      "Tela de Atualizações (menu superior) e correções de sexo/ordem dos itens";
}
